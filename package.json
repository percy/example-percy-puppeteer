{
  "private": true,
  "scripts": {
    "test": "percy exec -- mocha tests/ --exit"
  },
  "dependencies": {
    "expect": "^28.1.0",
    "todomvc-app-css": "^2.4.2"
  },
  "devDependencies": {
    "@percy/cli": "^1.1.0",
    "@percy/puppeteer": "^2.0.1",
    "http-server": "^14.1.0",
    "mocha": "^10.0.0",
    "puppeteer": "^13.7.0"
  }
}
