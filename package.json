{
  "private": true,
  "scripts": {
    "test": "percy exec -- mocha tests/ --exit"
  },
  "dependencies": {
    "expect": "^28.1.3",
    "todomvc-app-css": "^2.4.2"
  },
  "devDependencies": {
    "@percy/cli": "^1.2.1",
    "@percy/puppeteer": "^2.0.2",
    "http-server": "^14.1.1",
    "mocha": "^10.0.0",
    "puppeteer": "^15.2.0"
  }
}
